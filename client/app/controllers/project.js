(function () {
  'use strict';

  var project = function ($http, $state, project) {
    var ctrl = this;
  };

  project.$inject = ['$http', '$state', 'project'];

  angular.module('app')
    .controller('projectCtrl', project);

})();
